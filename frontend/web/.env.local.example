NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Publishable key (safe to expose client-side, NOT secret) for the embedded
# Stripe Elements paywall (PricingModal -> EmbeddedCheckout). Find it at
# https://dashboard.stripe.com/apikeys — same mode (test/live) as the
# backend's STRIPE_SECRET_KEY.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
